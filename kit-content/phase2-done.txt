
  ═══════════════════════════════════════════════════════════════════
  [OK] CVE CONFIRMED.
  ═══════════════════════════════════════════════════════════════════

  CVE-2018-1000861 -- Stapler unauthenticated RCE.

  In plain terms: this exact version of Jenkins has a hole that lets
  a stranger run commands on it with no login at all. That's the one
  we want, and this host is running a version it affects.

  You now have:
    - the host        jenkins-old.internal.crazy.ants
    - the hole         CVE-2018-1000861
    - a ready-made break-in for that hole

  Time to actually walk in.

  ─── next: break in and take the memo ──────────────────────────────

  The tool for this is metasploit. It keeps a ready-made attack for
  almost every known hole; you point it at the machine and it does
  the break-in. We've already loaded the attack for our hole.

  This is where most break-ins end up: one tool finds the exposed
  box, one reads off what's vulnerable, and metasploit walks through
  the door.

  Type the following in the terminal, one line at a time:

      cd msf
      cat brief

  `cat brief` lays out the handful of commands for the break-in.

  Stuck?  cat hint  -- no penalty.
