
  ═══════════════════════════════════════════════════════════════════
  [OK] CVE CONFIRMED.
  ═══════════════════════════════════════════════════════════════════

  CVE-2018-1000861 -- Stapler unauthenticated RCE.

  The Jenkins URL router can be tricked into invoking arbitrary
  methods on internal objects, including the Groovy script console
  -- with no authentication. The version on this host is in range.

  You now have:
    - the host (10.4.12.X)
    - the vulnerability (CVE-2018-1000861)
    - a known public exploit

  Time to actually walk in.

  ─── next ──────────────────────────────────────────────────────────

  To actually walk through the open door we've found, use
  metasploit. It's an exploitation framework -- a library of
  pre-scripted code snippets organized according to the particular
  CVE they exploit. Almost any time someone discovers a given
  vulnerability, someone else writes and logs a corresponding
  metasploit module to automate it. This helps everyone walk
  through the door once it's found. Pick a module. Point it at a
  host (in our case, 10.4.12.1).

  That's it. You're logged in. You get a shell.

  Most pentest workflows end here: nmap finds it, a scanner
  fingerprints it, metasploit walks through the door.

  Type each of these two commands in order, one at a time, hitting
  Enter after each. The first moves you into the phase 3 folder;
  the second prints the briefing for the metasploit walk.

      1.  cd /mnt/kit/03_metasploit
      2.  cat README

  Stuck?  cat hint  -- no penalty.

